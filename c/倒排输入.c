#include <stdio.h>
#define char MAXLINE 1000
int getline(char line[],char MAXLINE);
int copy(char longest[],char line[]);

int main(void)
{
    int c,i,max,len;
    max = 0;
    while((c = getline(line[],MAXLINE)) != EOF)
    {
             for(len = 0;c != '\n' && c <= MAXLINE && c != EOF;++len)
                  if(len > max)
                         max = len;
             if(c = '\n')
                  ++len;
             c 
    
    
    
    }   
} 
int getline()
{
    
    
    
}

int copy()
{   
    
    


} 
